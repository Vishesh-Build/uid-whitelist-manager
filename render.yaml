services:
  - type: web
    name: uid-whitelist-manager
    env: python
    buildCommand: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt
    startCommand: python axcanmol.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
